.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.metadata
pubspec.lock

# android/ jest wersjonowany w całości (manifest, gradle, ikony).
# Pliki lokalne (local.properties, .gradle) wyklucza android/.gitignore.
